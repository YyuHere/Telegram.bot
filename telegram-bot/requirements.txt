# Core Telegram library (MTProto)
pyrogram==2.0.106
TgCrypto==1.2.5          # Required C extension for fast crypto (speeds up Pyrogram)

# Voice chat streaming — DISABLED until music system is re-enabled
# pytgcalls==3.0.0.dev32
# tgcalls

# Audio download & extraction — DISABLED until music system is re-enabled
# yt-dlp>=2024.4.9

# Environment variable loading
python-dotenv>=1.0.1
