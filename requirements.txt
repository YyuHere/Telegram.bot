# Telegram Bot API library — async
python-telegram-bot==21.5

# Environment variable loading
python-dotenv==1.0.1

# Pyrogram — compatible with py-tgcalls 2.x
pyrogram==2.0.106
tgcrypto==1.2.5

# Voice chat streaming — 2.x series
py-tgcalls==2.2.11

# Audio download / stream URL resolution
yt-dlp>=2026.0.0

# JavaScript interpreter for yt-dlp challenge solver
js2py>=0.74

# Spotify Web API — used for metadata-guided YouTube search (Stage 2 fallback)
spotipy==2.24.0

# HTTP client — used for Anghami search API (Stage 3 fallback)
requests>=2.32.2,<3

# Unauthenticated YouTube Music catalog search (primary music source)
ytmusicapi>=1.12.2,<2
