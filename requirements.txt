# Telegram Bot API library — async, no API_ID/API_HASH needed
python-telegram-bot==21.5

# Environment variable loading
python-dotenv>=1.0.1

# Pyrogram MTProto client (Assistant userbot + full member scan)
pyrogram>=2.0.106
tgcrypto

# Voice chat streaming.
# py-tgcalls is the actively-maintained successor to pytgcalls 3.x.
# It provides the same `pytgcalls` Python module but without the unavailable
# native `tgcalls` binary dependency. Uses MediaStream instead of AudioPiped.
py-tgcalls

# Audio download / stream URL resolution
yt-dlp
py-tgcalls
pyrogram>=2.0.106
python-dotenv
python-telegram-bot==21.5
telegram
tgcrypto
yt-dlp
